# Supervisor handoff

Core baseline: `core-platform-v1`. Germany50 full selected-route result: 50 containers, 88 networks, 20 installed routes, all selected-path traffic runs successful. Final manifest and raw evidence paths are under `runs/final-evaluation/`.
