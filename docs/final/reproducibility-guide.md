# Reproducibility guide

Use the repository virtual environment, run unit tests, then invoke the checkpointed pipeline. Germany50 full mode validates a 4,224-entry route plan in dry-run; selected mode performs real traffic on the complete topology with batched per-container routes.
